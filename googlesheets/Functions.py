from Get_Sheet import main

def return_values(row,column,range_row,range_col):
    values = []
    for i in range(row):
        values.append([])
        for x in range(column):
            values[[]] = ((x,i))


a = return_values(10,10,1,1)